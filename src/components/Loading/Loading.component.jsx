import React from 'react';
import { LoadingFullContainer } from './styled';

function Loading() {
  return (
    <LoadingFullContainer>
      <p>Cargando</p>
    </LoadingFullContainer>
  );
}

export default Loading;
